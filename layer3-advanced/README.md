# layer3-advanced

Layer3 baseline is one-time copied from `layer2-stable@v0.1.0-stable.1` and then diverges.

- Implements advanced rendering modes
- Provides `POST /api/l3/build-direct`
- Provides `POST /api/l3/build-from-run`
