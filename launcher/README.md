# launcher

Single entrypoint for UI + API Gateway + Process Manager.

- Boot-time spawn: L1/L2/L3
- Health polling and restart policy are configured by `.env`
- This package must not import layer runtime code

